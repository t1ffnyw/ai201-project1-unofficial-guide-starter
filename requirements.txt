sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

gradio>=6.9.0

# Query interface — alternative:
# streamlit>=1.40.0

# Document ingestion — web scraping (scrape.py)
requests>=2.32
beautifulsoup4>=4.12
trafilatura>=2.0
lxml>=6.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
